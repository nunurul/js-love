const human = ["nurul", "nunu", "khoiriyah"];

for (names of human) {
  document.writeln(`<p>${names}</p>`);
}

const fullName = "nurul";

for (names of fullName) {
  document.writeln(`<p>${names}</p>`);
}
